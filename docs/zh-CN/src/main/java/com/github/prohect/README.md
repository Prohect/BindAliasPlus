# prohect

`com.github.prohect` —— 模组公共（main）源集的根包。包含 `ModInitializer` 入口点。

## 目录

| 名称 | 说明 |
|------|-------------|
| [BindAlias.java](BindAlias.java/README.md) | 主模组初始化器 —— 设置 `MOD_ID`、创建日志器、在 `onInitialize()` 中记录启动日志 |

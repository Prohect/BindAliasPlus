# prohect

`com.github.prohect` — the mod's root package for the common (main) source set. Contains the `ModInitializer` entry point.

## Contents

| Name | Description |
|------|-------------|
| [BindAlias.java](BindAlias.java/README.md) | The main mod initializer — sets `MOD_ID`, creates the logger, logs startup on `onInitialize()` |


BindAliasPlus\mc-decompile-sources is in .git\info\exclude,
search inside:
```bash
cd mc-decompile-sources/<branch>/ && grep <args>
```


TODO:
clear mod's internal states when quiting a server/integratedServer, recover game keyBindings changed by +lockKey or any other stuff this mod might change but the game itself wont clear...
